<body>
<header><p>label</p></header>
<%= form_with(model: @label, local: true) do |form| %>

  <div class="field">
    <%= form.label :label %><br>
    <%= form.text_field :label, id: :label_title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
</body>
